import 'package:flutter/material.dart';

class PassiveTasksList extends StatelessWidget {
  const PassiveTasksList({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('passive tasks list');
  }
}
