import 'package:flutter/material.dart';

import '../../../core/utils/choice.dart';

List<Choice> knowledgeBaseOverlayMenuItems(BuildContext context) {
  final List<Choice> choices = [
    Choice(title: 'kb', icon: Icons.abc, onTap: () {}),
  ];
  return choices;
}
